# Polymarket 跟单机器人 — 运行说明

本文档说明如何在本机安装、配置并运行本项目。

---

## 一、环境要求

| 项目 | 说明 |
|------|------|
| **Python** | 3.10 或更高版本 |
| **MongoDB** | 用于存储用户活动与仓位，可用 [MongoDB Atlas](https://www.mongodb.com/cloud/atlas/register) 免费版 |
| **Polygon 钱包** | 跟单使用的钱包：需有 USDC（交易）和 POL/MATIC（Gas） |
| **RPC** | Polygon 主网 RPC，可从 [Infura](https://infura.io)、[Alchemy](https://www.alchemy.com) 等获取免费额度 |

---

## 二、安装步骤

### 1. 克隆仓库并进入目录

```bash
git clone <本仓库地址>
cd polymarket-trading-bot-python
```

### 2. 创建虚拟环境（推荐）

```bash
python3 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

### 4. 配置环境变量

**方式 A：使用交互式配置向导（推荐）**

```bash
python -m src.scripts.setup.setup
```

按提示填写钱包、MongoDB、RPC 等，会生成项目根目录下的 `.env` 文件。

**方式 B：手动配置**

1. 复制示例配置：
   ```bash
   cp .env.example .env
   ```
2. 用编辑器打开 `.env`，填写必填项（见下文「配置说明」）。

---

## 三、配置说明

`.env` 中与运行直接相关的主要变量如下。

### 必填项

| 变量 | 说明 | 示例 |
|------|------|------|
| `USER_ADDRESSES` | 要跟单的交易员钱包地址（目标地址集），多个用逗号或 JSON 数组 | `'0xABC...,0xDEF...'` |
| **跟单钱包（二选一）** | | |
| `PROXY_WALLET` | 单钱包：你的 Polygon 跟单地址 | `'0x123...'` |
| `PRIVATE_KEY` | 单钱包：上述地址的私钥（**不要**带 `0x` 前缀） | `abc123...` |
| **或多钱包（轮询）** | | |
| `PROXY_WALLETS` | 跟单地址集，多个用逗号或 JSON 数组，与 `PRIVATE_KEYS` 顺序一致 | `'0x1...,0x2...,0x3...'` |
| `PRIVATE_KEYS` | 与 `PROXY_WALLETS` 一一对应的私钥，逗号分隔 | `key1,key2,key3` |
| `MONGO_URI` | MongoDB 连接串 | `mongodb+srv://user:pass@cluster...` |
| `RPC_URL` | Polygon RPC 地址 | `https://polygon-mainnet.infura.io/v3/KEY` |
| `USDC_CONTRACT_ADDRESS` | Polygon 上 USDC 合约（一般不需改） | `0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174` |
| `CLOB_HTTP_URL` | Polymarket CLOB API（一般不需改） | `https://clob.polymarket.com/` |
| `CLOB_WS_URL` | Polymarket CLOB WebSocket（一般不需改） | `wss://ws-subscriptions-clob.polymarket.com/ws` |

说明：跟单钱包须配置**其中一种**。单钱包用 `PROXY_WALLET` + `PRIVATE_KEY`；多钱包用 `PROXY_WALLETS` + `PRIVATE_KEYS`（每笔目标交易按顺序由下一个跟单地址执行，循环轮询）。若两套都配置，以 `PROXY_WALLETS`/`PRIVATE_KEYS` 为准。

### 常用可选项

| 变量 | 说明 | 默认 |
|------|------|------|
| `FETCH_INTERVAL` | 拉取新交易的间隔（秒） | `1` |
| `TOO_OLD_TIMESTAMP` | 忽略超过多少小时的交易（小时） | `24` |
| `COPY_STRATEGY` | 跟单策略：`PERCENTAGE` / `FIXED` / `ADAPTIVE` | `PERCENTAGE` |
| `COPY_SIZE` | 跟单比例或固定金额（由策略决定） | `10.0`（如 10%） |
| `TRADE_AGGREGATION_ENABLED` | 是否开启小单聚合 | `false` |
| `TRADE_AGGREGATION_WINDOW_SECONDS` | 聚合时间窗口（秒） | `300` |

更多项见 `.env.example` 内注释。

---

## 四、运行前检查

在首次运行或更换环境后，建议先做一次系统自检：

```bash
python -m src.scripts.setup.system_status
```

会检查 MongoDB、RPC、余额、CLOB 等是否正常。若有报错，先按提示修复再启动主程序。

---

## 五、启动机器人

在项目根目录执行：

```bash
python -m src.main
```

启动后将会：

1. 连接 MongoDB
2. 初始化 CLOB 客户端
3. 启动 **交易监控**（从 Polymarket data-api 拉取被跟单者的交易并写入数据库）
4. 启动 **交易执行器**（根据策略对符合条件的交易下单）

正常运行时终端会持续输出状态与交易日志。**停止运行**：在终端按 `Ctrl+C`，程序会做一次优雅退出（停止监控与执行、关闭数据库连接）。

---

## 六、其他常用命令

| 命令 | 说明 |
|------|------|
| `python -m src.scripts.setup.setup` | 重新运行配置向导，覆盖/备份现有 `.env` |
|`python3 scripts/sync_env_from_example.py`|
| `python -m src.scripts.setup.system_status` | 系统状态检查（MongoDB、RPC、余额、CLOB 等） |
| `python -m src.main` | 启动跟单主程序 |

更多脚本（模拟、研究、仓位管理等）可在 `src/scripts/` 下按需查看与运行。

---

## 七、注意事项

1. **私钥与 .env**  
   `.env` 含私钥，切勿提交到 Git。项目已通过 `.gitignore` 忽略 `.env` 和 `.env.backup`。

2. **实战前必读**  
   下单逻辑依赖 CLOB 客户端实现。当前仓库中 CLOB 下单可能为占位实现，**实盘前请阅读 [SECURITY.md](SECURITY.md)**，按说明自行实现并审计 CLOB 下单流程后再用于真实资金。

3. **资金与风险**  
   仅用可承受损失的金额运行；建议先用小额或模拟验证行为是否符合预期。

4. **进程与网络**  
   保持本机网络稳定；若用云服务器，建议用 `screen` 或 `tmux` 等保持进程在后台运行。

---

## 八、故障排查

- **启动报错「Missing required environment variables」**  
  检查 `.env` 是否在项目根目录，且必填变量均已填写（无空字符串）。

- **MongoDB 连接失败**  
  检查 `MONGO_URI`、网络与 IP 白名单（若使用 Atlas）。

- **RPC 或余额检查失败**  
  确认 `RPC_URL` 有效、钱包有 POL（Gas）和 USDC。

- **没有跟单成交**  
  确认 `USER_ADDRESSES` 正确、被跟单者有新交易，并阅读 [SECURITY.md](SECURITY.md) 确认 CLOB 是否已实现为真实下单。

---

以上为运行本项目的完整说明。配置与运行中若遇问题，可结合 `README.md` 与 `SECURITY.md` 一起查阅。
